
server {
    listen 3306;
    proxy_pass medhack-frontiers-db-staging:3306;

    proxy_connect_timeout 30s;
    proxy_timeout 10m;
}

# bittorrent proxy

server {
    listen 6881;
    proxy_pass qbittorrent:6881;
}
