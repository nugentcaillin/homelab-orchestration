networks:
  shared:
    external: true
    name: proxy-bridge 

services:
  personal-site:
    container_name: personal-site
    image: caillinnugent/personal-site:latest
    ports:
      - "8080:8080"
    networks:
      - shared
    restart: unless-stopped
